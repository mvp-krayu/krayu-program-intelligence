# Block K — BlueEdge Rerun Summary
## Contract: PI.LENS.END-TO-END-RERUN.BLUEEDGE.01
## Date: 2026-04-30
## Verdict: BLUEEDGE_RERUN_SUCCESS

---

## Did the Rerun Reproduce the Baseline?

**Yes — fully.**

All 14 structural and decision checks match the prior validated baseline exactly:
- Score: 60 / CONDITIONAL / INVESTIGATE
- 17 semantic domains (5 backed, 12 semantic-only, 5 clusters)
- 13 structural groups
- PZ-001 / DOM-04 / backend_app_root
- PSIG-001/002/004 active, PSIG-006 baseline, PSIG-003/005 not activated

Report content is bit-for-bit identical to the baseline modulo navigation hrefs — the only difference is that `run_blueedge_rerun_01` is embedded instead of `run_blueedge_productized_01_fixed` in navigation link run_id parameters. Confirmed by unified diff: zero non-runid differences across all reports.

graph_state.json MD5 is identical across both runs — d3-force simulation is deterministic given the same vault topology.

---

## Is the System Deterministic?

**Yes.**

Same source artifacts → same content output. The generator produces identical content across independent invocations from the same artifact pipeline. Navigation hrefs vary by run_id (by design and correct). All other content is deterministic.

---

## Is the Generator Stable?

**Yes.**

- T2 is fully generator-driven: `_resolve_vault_index_for_graph()` + `--vault-index-path` eliminates the DEV-T2-VAULT patch dependency
- All 4 reports (8 files + publish variants) generated by the generator in a single `--output-root` command
- No manual patches, no fallback paths, no post-generation fixups

---

## Does the Selector Contract Hold?

**Yes.**

- `clients/blueedge/lens/selector/selector.json` updated: `current_run = run_blueedge_rerun_01`
- `available_runs.json` now contains 2 runs: `run_blueedge_productized_01_fixed` and `run_blueedge_rerun_01`
- `current/` updated to mirror `run_blueedge_rerun_01` reports
- Prior run (`run_blueedge_productized_01_fixed`) preserved in `runs/` and `available_runs`
- Navigation requires no selector context — hrefs are self-contained per prior contract clarification

---

## Is the System Ready for the Second Client?

**Yes.**

The system is client-agnostic and reproducible. To run for a second client:

```bash
python3 scripts/pios/lens_report_generator.py \
  --client <second_client_id> \
  --run-id <second_client_run_id> \
  --package-dir clients/<second_client_id>/psee/runs/<run_id>/vault \
  --output-root clients/<second_client_id>/lens/runs/<run_id> \
  ...
```

The second client needs:
1. Equivalent vault artifacts (canonical_topology.json, gauge_state.json, etc.)
2. 41.x grounded projections (signal_projection.json, pressure_zone_projection.json)
3. At least one entry in `app/gauge-product/public/vault/<client>/` for graph export (or any app vault entry for fallback)

---

## Validation Summary

10/10 VF checks PASS. See validation_log.json.
