'use strict';

const NARRATIVE_WHY_RENDER_FIXTURE = {
  why_primary_statement: 'Structural pressure concentrates in the primary delivery domain, where cluster load exceeds the distribution pattern established in the baseline topology.',
  renderState: 'EXECUTIVE_READY',
  densityClass: 'EXECUTIVE_DENSE',
  expected_show_why_statement: true,
  expected_why_default_expanded: true,
};

module.exports = { NARRATIVE_WHY_RENDER_FIXTURE };
