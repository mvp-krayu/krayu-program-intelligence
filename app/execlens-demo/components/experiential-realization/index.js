'use strict';

const {
  resolveRevealSequence,
  resolveMotionProfile,
  resolveDensityTransition,
  resolveEvidenceEmphasis,
  getPhaseIndex,
  isBlockedOrDiagnosticFirst,
  MOTION_GOVERNANCE_INVARIANTS,
} = require('./MotionSemanticController');

const { resolveExperientialDensityLayout } = require('./IntelligenceDensityOrchestrator');

module.exports = {
  resolveRevealSequence,
  resolveMotionProfile,
  resolveDensityTransition,
  resolveEvidenceEmphasis,
  getPhaseIndex,
  isBlockedOrDiagnosticFirst,
  MOTION_GOVERNANCE_INVARIANTS,
  resolveExperientialDensityLayout,
};
